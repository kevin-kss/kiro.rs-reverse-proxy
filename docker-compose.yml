services:
  kiro-rs:
    # build: .
    image: ghcr.io/kevin-kss/kiro.rs-reverse-proxy:latest
    container_name: kiro-rs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8990:8990"
    volumes:
      - ./config/:/app/config/
    restart: unless-stopped
