name: Release MacOS Build
permissions:
  contents: write

on:
  push:
    tags:
      - "v*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install frontend dependencies
        working-directory: admin-ui
        run: pnpm install

      - name: Build frontend
        working-directory: admin-ui
        run: pnpm build

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "rust-cache-macos"
          cache-on-failure: true

      - name: Build arm64
        run: cargo build --release --target aarch64-apple-darwin

      - name: Build amd64
        run: cargo build --release --target x86_64-apple-darwin

      - name: Prepare artifacts
        run: |
          cp target/aarch64-apple-darwin/release/kiro-rs kiro-rs-darwin-arm64
          cp target/x86_64-apple-darwin/release/kiro-rs kiro-rs-darwin-amd64

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            kiro-rs-darwin-arm64
            kiro-rs-darwin-amd64
          draft: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
